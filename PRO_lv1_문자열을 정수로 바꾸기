func solution(_ s:String) -> Int {
    // if s[s.startIndex] == "-" {
    //     let i = s.index(s.startIndex, offsetBy: 1)
    //     let str = String(s[i...])
    //     return 0 - Int(str)!
    // } else {
    //     return Int(s)!
    // }
    return Int(s)!
}
